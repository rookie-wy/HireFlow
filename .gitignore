# =============================================================================
# HireFlow（AI 招聘 Agent 系统）— 仓库忽略规则
#
# 🔒 第一原则：**任何含密钥 / 环境变量 / 本机路径 / 个人数据的文件都不进仓库**。
#    提交前请用 `git status --short` 与 `git diff --cached --name-only` 复核。
# =============================================================================

# ---------------------------------------------------------------- 密钥与环境变量（务必保持）
.env
.env.*
!.env.example
**/.env
**/.env.*
!**/.env.example
*.pem
*.key
*.p12
*.pfx
credentials.json
service-account*.json

# ---------------------------------------------------------------- 依赖与虚拟环境
**/.venv/
**/venv/
**/node_modules/
__pycache__/
*.py[cod]
*.egg-info/
.pnpm-store/

# ---------------------------------------------------------------- 本地模型权重（约 6.5GB，运行期挂载）
agent/models/
**/models/bge-m3/
**/models/bge-reranker-v2-m3/
*.safetensors
*.onnx
*.gguf
model_cache/

# ---------------------------------------------------------------- 构建产物
frontend/dist/
frontend/.vite/
backend/server
backend/server.exe
tmp/*.exe
*.tsbuildinfo

# ---------------------------------------------------------------- 运行日志与临时数据
logs/
logs-*.log
*.log
tmp/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.gptcache_data/
*.sqlite3
rerank_inputs.json

# ---------------------------------------------------------------- 旧单体实现（v4，仅本地参考，不入库）
# 新仓库只保留三服务架构；旧代码可从远端历史（src/ 时代提交）或本地备份取回。
/src/

# ---------------------------------------------------------------- 个人 / 本机数据（简历样本、截图等）
resume_*.pdf
*.docx
shots/
.idea/
.vscode/
.DS_Store
Thumbs.db
