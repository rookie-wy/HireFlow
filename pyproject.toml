[project]
name = "pythonproject2"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    # ===== 核心 =====
    "fastapi==0.115.0",
    "uvicorn[standard]==0.30.1",
    "slowapi==0.1.9",
    "pydantic==2.8.2",
    "pydantic-settings==2.4.0",
    "python-jose[cryptography]==3.3.0",
    "passlib[bcrypt]==1.7.4",
    "python-multipart==0.0.9",
    # Agent 编排与 LLM
    "langgraph==0.2.0",
    "litellm>=1.80.0",
    "openai>=1.109.1",
    "json-repair>=0.61.2",
    # 数据
    "pymysql==1.1.0",
    "dbutils==3.1.0",
    "redis==5.0.8",
    "chromadb>=1.5.9",
    # 检索 / 嵌入 / 解析
    "flagembedding>=1.4.0",
    "sentence-transformers>=5.6.0",
    "pymupdf>=1.24.14",
    # 健壮性
    "tenacity==8.5.0",
    "pybreaker>=1.4.1",
    "requests>=2.34.2",
    # 前端原型
    "streamlit>=1.56.0",
    # 测试
    "pytest==8.2.0",
    "pytest-asyncio==0.24.0",
    "pytest-mock==3.14.0",
    # ===== 后置（代码已做容错，可安全移除；规模化时再启用）=====
    # "langfuse==2.47.0",
    # "opentelemetry-api==1.26.0",
    # "opentelemetry-sdk==1.26.0",
    # "prometheus-client==0.20.0",
    # "gptcache[sbert]==0.1.40",
    # "celery==5.3.6",
]

# ===== 已砍掉（勿再装）=====
#   PDF 解析六件套已收敛为 pymupdf（见 app/infrastructure/models/mineru_client.py）：
#   "magic-pdf>=1.3.12",
#   "mineru>=3.4.2",
#   "paddleocr>=3.7.0",
#   "paddlepaddle>=3.3.1",
#   "pypdf2==3.0.1",
#   "pdfplumber>=0.11.7",
#   安全扫描（声明但代码零引用，纸面安全）：
#   "guardrails-ai>=0.6.8",

[[tool.uv.index]]
url = "https://repo.huaweicloud.com/repository/pypi/simple"
default = true
trusted-host = "repo.huaweicloud.com"